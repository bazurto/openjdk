# Terraform

## Upload Releases
1. Edit `versions.txt` file with list of releases to create.  Remove any existing releases to avoid overriding.  Also, make sure the most reacent release is at the bottom.
2. Run `./releases.sh` script

